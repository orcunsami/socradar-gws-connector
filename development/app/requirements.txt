# Pinned to tested versions for reproducible builds (supply-chain). The CI step in deploy/supply-chain.md
# adds full hash-pinning (--generate-hashes / --require-hashes) + SBOM over the transitive tree.
fastapi==0.136.3
uvicorn[standard]==0.49.0
jinja2==3.1.6
authlib==1.7.2
itsdangerous==2.2.0
httpx==0.28.1
python-multipart==0.0.30
google-auth==2.53.0
google-cloud-firestore==2.27.0   # durable storage backend (STORAGE_BACKEND=firestore)
cryptography==48.0.0             # Fernet at-rest encryption of the feed key (FERNET_KEY)
google-cloud-bigquery==3.41.0    # optional analytics export (ANALYTICS_BIGQUERY=true)
pydantic-settings==2.14.1
